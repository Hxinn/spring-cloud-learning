server:
  port: 3355
spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      name: config
      profile: test
      uri: http://127.0.0.1:3344
eureka:
  client:
    service-url:
      defaultZone: http://eurekaA:8001/eureka/